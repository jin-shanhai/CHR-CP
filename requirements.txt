openai==1.59.6
python-dotenv==1.0.1
pyyaml==6.0.2
tenacity==9.0.0
loguru==0.7.3
numpy>=2.0,<2.3
pandas==2.2.3
tqdm==4.67.1
rich==13.9.4
pytest==8.3.4
pytest-asyncio==0.25.0
# The project uses Zhipu's OpenAI-compatible endpoint; keep the SDK optional
# and install a published compatible build when direct SDK access is needed.
zhipuai>=2.1.5.20250825
